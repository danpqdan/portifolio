name: CI

# CI do portfolio pessoal (frontend React/Vite).
# Backend → DSPlayAnalytics/backend; Infra → DSPlayAnalytics/ark.

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'docker-compose.yml'
      - '.github/workflows/ci.yml'

permissions:
  contents: read

jobs:
  compose-config:
    name: docker-compose.yml valido
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
      - name: Resolver variaveis dummy e validar
        run: |
          cat > .env <<'EOF'
          INFLUXDB_INIT_PASSWORD=ci-dummy
          INFLUXDB_TOKEN=ci-dummy
          SECRET_KEY=ci-dummy
          ADMIN_API_TOKEN=ci-dummy
          POSTGRES_PASSWORD=ci-dummy
          EOF
          mkdir -p backend
          : > backend/.env
          docker compose config --quiet

  frontend:
    name: Frontend (lint + test + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install
        run: npm ci
      - run: npm run lint
      - run: npm run test
      - run: npm run build

  frontend-docker:
    name: Frontend (docker build multi-stage)
    runs-on: ubuntu-latest
    # Gate contra regressao do Dockerfile: o fix "vite: not found" (NODE_ENV=production
    # antes do npm ci pulava devDependencies) so quebrou na hora do docker compose up
    # em producao. Rodar `docker build` aqui pega esse tipo de falha no PR.
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4
      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3
      - name: Build da imagem frontend (args dummy)
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f  # v6
        with:
          context: ./frontend
          push: false
          load: false
          build-args: |
            VITE_API_URL=http://ci-dummy:5000
            VITE_WEBSOCKET_URL=http://ci-dummy:5000
            VITE_DEBUG=false
          secrets: |
            NODE_AUTH_TOKEN=${{ secrets.NPM_AUTH_TOKEN }}

