; Copie para inventory.ini e ajuste IP/chave.
;
; ESTRATEGIA DE BOOTSTRAP:
;   1a execucao (servidor zerado, so root existe):
;     ansible-playbook -i inventory.ini playbook.yml --tags base -u root
;     -> cria deploy user + propaga authorized_keys de root
;
;   Execucoes seguintes (deploy ja existe):
;     ansible-playbook -i inventory.ini playbook.yml
;     -> usa ansible_user=deploy definido em [analytics:vars]

[analytics]
129.121.55.29 ansible_port=22022 ansible_ssh_private_key_file=~/.ssh/vpn

[analytics:vars]
ansible_user=deploy
ambiente=production
dominio=dsplayground.com.br
letsencrypt_email=danieltisantos@gmail.com
