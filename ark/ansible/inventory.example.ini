; Copie para inventory.ini e edite com os dados reais.

[analytics]
; hostname ou IP do servidor de producao
; servidor01.example.com  ansible_user=deploy  ansible_ssh_private_key_file=~/.ssh/id_deploy
127.0.0.1 ansible_user=deploy

[analytics:vars]
; ambiente alvo (dev/test/staging/production)
ambiente=production
; dominio publico que aponta para este servidor
dominio=analytics.example.com
; email para Let's Encrypt
letsencrypt_email=ops@example.com
